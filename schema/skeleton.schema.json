{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moho2svg.local/schema/skeleton.schema.json",
  "title": "Moho skeleton and bones",
  "description": "A BoneLayer's (or SwitchLayer's) `skeleton` field. See docs/moho-project-file-format.md §9 for the deformation model (rigid vs flexible/region binding) and the module docstring's BONE DEFORMATION and SMART BONES sections.",
  "$defs": {

    "Skeleton": {
      "type": "object",
      "required": ["type", "binding_mode", "bones"],
      "properties": {
        "type": { "const": "Skeleton" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "binding_mode": { "type": "integer", "description": "Observed values: 1 (large majority), 2 (rare). Meaning not decoded; moho2svg.py never branches on it." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "bones": { "type": "array", "items": { "$ref": "#/$defs/Bone" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:3043 in Skeleton._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "bones_groups": {
          "type": "array",
          "items": { "$ref": "#/$defs/BoneGroup" },
          "description": "Observed only rarely, and empty in every sample seen. Presumably Vitruvian Bones (a Moho Pro 13.5 feature - animatable per-group 'which bone is active' switching, like a SwitchLayer scoped to a subset of one skeleton's bones) - matched at the MECHANISM level by Moho's own scripting API, M_Skeleton::CountGroups()/Group(id) returning M_BoneGroup {name, AnimVal fActiveBone, bool fEnabled}, though not confirmed as the same field at the serialization level. See docs/moho-rigging-and-deformation.md section 4b. Read by moho2svg.py only to WARN when non-empty (Skeleton.bones_groups); the groups themselves are not parsed or applied."
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:3046 in Skeleton._build (trace: out/traced_keys.json, see tools/trace_fields.py)"}
      },
      "additionalProperties": true
    },

    "BoneGroup": {
      "type": "object",
      "description": "One entry of Skeleton.bones_groups - see that field's own description for the (unconfirmed at the serialization level) Vitruvian Bones mechanism. Not applied by moho2svg.py; the whole array is only checked for non-emptiness.",
      "properties": {
        "type": { "const": "BoneGroup" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "name": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2681 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "enabled": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5025 in Layer.physics_dynamic (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "bones": { "type": "array", "items": { "type": "integer" }, "description": "Indices into the enclosing Skeleton.bones array - this group's member bones." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:3043 in Skeleton._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "active_bone": {
          "$ref": "channel.schema.json#/$defs/ValChannel",
          "description": "Which member bone is currently active, as an animatable index into `bones` (Moho's own scripting header names the equivalent member M_BoneGroup::fActiveBone, an AnimVal). Never read by moho2svg.py - see bones_groups above."
        , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"}
      },
      "additionalProperties": true
    },

    "Bone": {
      "type": "object",
      "description": "One bone. A flat list (Skeleton.bones) - `parent` is an index into the SAME list, not a tree. Parents are not guaranteed to appear earlier in the list than their children. Every constraint/IK/physics/control field below (angle_control_*, pos_control_*, scale_control_*, ik_*, min/max_constraint, and every *_dynamics/*_force/physics_* field) is read by moho2svg.py in the sense that the document model exposes it, but NOT applied when rendering - see docs/moho-project-file-format.md §9 KNOWN GAPS. They matter only for posing live in the Moho editor; the resulting angle/position is already baked into anim_angle/anim_pos by the time the document is saved, EXCEPT for anim_parent, which can genuinely diverge from the static `parent` field (see below).",
      "required": ["type", "name", "parent", "length", "strength"],
      "properties": {
        "type": { "const": "Bone" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "name": { "type": "string", "description": "Also how a Smart Bone dial is matched: a bone counts as a dial if its own name appears in the enclosing BoneLayer's `actions` registry - see docs/moho-project-file-format.md §11." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2681 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "parent": { "type": "integer", "description": "-1 for a root bone, otherwise an index into this same bones array." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4391 in MeshPoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "length": { "type": "number", "description": "In document units (2 units span the canvas height)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2954 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "strength": { "type": "number", "description": "Influence radius for flexible binding. 0 disables this bone's influence entirely, checked before any weight-falloff calculation - Moho's own \"this bone does not deform this mesh\" gate." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2955 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "offset": {
          "type": "object",
          "description": "Plain (not a channel).",
          "properties": { "x": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:1544 in Vec2.of (trace: out/traced_keys.json, see tools/trace_fields.py)"}, "y": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:1544 in Vec2.of (trace: out/traced_keys.json, see tools/trace_fields.py)"} }
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2981 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "anim_pos": { "$ref": "channel.schema.json#/$defs/Vec2Channel", "description": "Animated position, relative to the parent bone." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2956 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "anim_angle": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Animated angle in radians." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2957 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "anim_scale": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "A SINGLE, uniform scale (unlike a layer's transforms.scale, which is per-axis) - see Skeleton.world_matrices in the module docstring." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2958 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "anim_parent": {
          "$ref": "channel.schema.json#/$defs/ValChannel",
          "description": "Animated reparenting: the value at each keyframe is a bone INDEX (or -1), letting a bone change parent mid-animation (the Reparent Bone tool). moho2svg.py uses the static `parent` field instead and ignores this - confirmed harmless in the reverse-engineering sample set only because every sampled anim_parent channel has exactly one keyframe whose value equals the bone's own static `parent` (zero mismatches across 550+ bones, including in a document created specifically to demonstrate the Reparent Bone tool) - a document that actually keyframes a mid-animation reparent would be rendered wrong."
        , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},

        "scaling_mode": { "type": "integer", "description": "Observed values: 0, 2. Meaning not decoded; possibly related to the intentionally-preserved asymmetric bone scale in Skeleton.world_matrices (only the matrix's first column is scaled by anim_scale)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2960 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "squash_stretch_scaling": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2961 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "max_auto_scaling": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2962 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "hidden": { "type": "boolean", "description": "Editor state." , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."},
        "shy": { "type": "boolean", "description": "Editor state." , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."},
        "selected": { "type": "boolean", "description": "Editor state." , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."},
        "bone_label_showing": { "type": "boolean" , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."},
        "bone_tags": { "type": "integer" , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."},

        "fixed_angle": {
          "type": "boolean",
          "description": "\"Independent angle\" (65 bones / 11 documents): a flagged bone keeps its parent's position but not the parent's departure from its own rest rotation.",
          "x-moho-disposition": "MODELLED",
          "x-moho-evidence": "moho2svg.py Skeleton.world_matrices NOTE ON INDEPENDENT ANGLE; measured by rendering TransformBoneTool.animeproj with Moho twice, flag forced on/off"
        },
        "min_constraint": { "type": "number", "description": "In radians. Only meaningful in the Moho editor while posing - the resulting angle is already baked into anim_angle." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "max_constraint": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "constraints": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},

        "ik_lock": { "$ref": "channel.schema.json#/$defs/BoolChannel" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "ik_global_angle": { "$ref": "channel.schema.json#/$defs/ValChannel" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "ik_parent_target": { "$ref": "channel.schema.json#/$defs/Vec2Channel" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "ignored_by_ik": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "bone_enable_arc_solver": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "target_bone": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "The Target Bone tool's own target reference." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2959 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "angle_control_parent": { "type": "integer", "description": "Index into this same bones array of the bone that drives this one's angle, or -1 for none (manual ch. 5.01/23.03, 'Angle control bone'). APPLIED: the controlled bone adds the controller's own departure from its rest (frame 0) value, times angle_control_scale, sampled angle_control_delay frames earlier. Verified against Moho's own renders - see docs/moho-rigging-and-deformation.md section 3.3. Observed non-trivial targets in bone-control-focused documents (e.g. ControlBones.animeproj, IndependentAngle.animeproj)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2971 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "angle_control_scale": { "type": "number", "description": "How much of the controller's movement to follow (manual ch. 23.03: '100% ... exactly the same way ... 200% ... double'). 1.0 = follow exactly; negative mirrors." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2972 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "angle_control_delay": { "type": "integer", "description": "Frames of delay: the controlled bone repeats what the controller did this many frames EARLIER. Sign verified on Whale.mohoproj bone 26 (delay 4), the corpus' one live delayed constraint: rms 0.598 deg for t-delay vs 6.272 for t and 9.524 for t+delay." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2973 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "pos_control_parent": { "type": "integer", "description": "Same as angle_control_parent, for the bone's translation. Read and applied by the same code path." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2974 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "pos_control_scale": {
          "type": "object",
          "description": "Plain (not a channel) - a per-axis gain, unlike the scalar used by the angle and scale families.",
          "properties": { "x": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:1544 in Vec2.of (trace: out/traced_keys.json, see tools/trace_fields.py)"}, "y": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:1544 in Vec2.of (trace: out/traced_keys.json, see tools/trace_fields.py)"} }
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2975 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "pos_control_delay": { "type": "integer", "description": "As angle_control_delay, for the position family." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2976 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "scale_control_parent": { "type": "integer", "description": "Same as angle_control_parent, for the bone's stretch/scale." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2977 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "scale_control_scale": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2978 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "scale_control_delay": { "type": "integer", "description": "As angle_control_delay, for the scale family." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2979 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "angle_weight": { "type": "number", "description": "Observed -1.0 or 1.0." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "pos_weight": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "scale_weight": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},

        "flip_h": { "$ref": "channel.schema.json#/$defs/BoolChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4542 in Transform.__init__ (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "flip_v": { "$ref": "channel.schema.json#/$defs/BoolChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4543 in Transform.__init__ (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "bone_dynamics": { "$ref": "channel.schema.json#/$defs/BoolChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2965 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "angle_dynamics": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2966 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "pos_dynamics": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "scale_dynamics": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "wind_dynamics": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2967 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "spring_force": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2968 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "damping_force": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2969 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "torque_force": { "type": "number" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2970 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "pos_spring_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "pos_damping_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "pos_torque_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "scale_spring_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "scale_damping_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "scale_torque_force": { "type": "number" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "physics_lock_tip": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "physics_radius": { "type": "number", "description": "Observed constant -1.0 in every sample." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "physics_return_to_zero": { "type": "boolean" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "physics_torque": { "type": "number", "description": "Observed constant 10.0 in every sample." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "physics_motor_speed": { "$ref": "channel.schema.json#/$defs/ValChannel" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"}
      },
      "additionalProperties": true
    }
  }
}
