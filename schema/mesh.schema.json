{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moho2svg.local/schema/mesh.schema.json",
  "title": "Moho vector mesh (points, curves, shapes)",
  "description": "A MeshLayer's `mesh` field: the vector artwork itself. See docs/moho-project-file-format.md §7 for the full field-by-field evidence, and §5.1/§7.5 for why the Bezier control points are reconstructed from smoothness/weight/offset rather than stored directly.",
  "$defs": {

    "Mesh": {
      "type": "object",
      "required": ["type", "points", "curves", "shapes"],
      "properties": {
        "type": { "const": "Mesh" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "points": { "type": "array", "items": { "$ref": "#/$defs/MeshPoint" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4526 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "curves": { "type": "array", "items": { "$ref": "#/$defs/Curve" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4527 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "shapes": { "type": "array", "items": { "$ref": "#/$defs/Shape" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4528 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "groups": {
          "type": "array",
          "description": "Named point groups (an editor selection convenience, e.g. \"Right Hand\", \"top lip\") - not read by moho2svg.py. Distinct from a layer's flexi_bone_subset, which names BONE indices, not point indices. Present on 4,969 sites/76 documents, non-empty (real named groups) on 2 documents (Snow_wars/01 opening.moho, Others/ReparentBone.animeproj). Probed on 01 opening.moho's own sites, forced to a synthetic `[{name,points}]` entry: inert.",
          "items": { "$ref": "#/$defs/PointGroup" }
        , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: groups=[{name:\"probe_group\",points:[0,1,2]}], 67 sites, Snow_wars/01 opening.moho frame 0, inert. Not read by moho2svg.py."},
        "shape_order": {
          "$ref": "channel.schema.json#/$defs/StringChannel",
          "description": "A \"|\"-joined ascending id registry of shape.id values - NOT a z-order, despite the name. The real z-order (back to front) is simply the order shapes already appear in `shapes`. Read into the model (Mesh._build), but never applied - Mesh.draw_order() deliberately returns `shapes` unchanged and ignores this field, on three pieces of evidence (ascending-ID signature on 47/49 disagreeing meshes, breaks combo_mode adjacency if trusted, matches an earlier docstring experiment); see that method's own docstring. See docs/moho-project-file-format.md §7.9."
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4529 in Mesh._build; deliberately not applied by Mesh.draw_order (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "anim_shape_order": { "type": "boolean", "description": "Presumably enables keyframing shape_order (itself MODELLED-unreachable - see that field's own description; Mesh.draw_order ignores it regardless). Observed false on 4,955 of 4,969 corpus meshes (76-document scan), true on 14 - across 7 documents, all `Snow-girl/Snow-girl-cut*.mohoproj` (cut7, cut8, cut10, cut11, cut12, cut14, cut51) (not \"false in every sample\", the earlier text here). Forcing it true on every one of Bandit.mohoproj's 21 meshes (all stored false, so a real change) left frame 25 byte-identical - consistent with shape_order itself never being consulted." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: anim_shape_order=true, 21 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py (grepped, only prose mentions)."},
        "next_shape_id": { "type": "integer", "description": "The id allocator's next value. Not semantically meaningful for a reader. Forcing it to 999 on every one of Bandit.mohoproj's 21 meshes left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: next_shape_id=999, 21 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py."},
        "curve_interpretation": {
          "type": "integer",
          "description": "Meaning not decoded; moho2svg.py does not branch on it. Observed values: 1 on 4,912 of 4,969 corpus meshes (76-document scan), 0 on 57 - concentrated in DarkMan.mohoproj (52), plus one each in Others/AddBone.animeproj (x2), Others/IndependentAngle.animeproj, Others/MaximumIKStrethching.animeproj, Others/TargetBone.animeproj - a real, if rare, alternate value, not a synthetic edge case. Forcing it from 1 to 0 on every one of Bandit.mohoproj's 21 meshes (all stored 1) left frame 25 byte-identical."
        , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: curve_interpretation=0, 21 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py."},
        "skia_scaling": {
          "type": "number",
          "description": "Observed constant -1.0 across every sampled mesh - present on 227 sites/9 documents (Boar, Clay_Crocodile, Cocon, DarkMan, Lute, Night_Boy, SketchBone.mohoproj, Spacewoman, Whale), absent from the rest (an older-generation field, like several other newer/older-format splits in this schema). Presumably a cached scale factor for Moho's internal Skia-based rasterizer (a sentinel meaning \"not yet computed\"), not a document-authored value. Not read by moho2svg.py. Probed on Boar.mohoproj's own 3 real sites, forced to 2.5: inert."
        , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: skia_scaling=2.5, 3 sites, Boar.mohoproj frame 0, inert. Not read by moho2svg.py."}
      },
      "additionalProperties": true
    },

    "MeshPoint": {
      "type": "object",
      "required": ["type", "position", "width"],
      "properties": {
        "type": { "const": "Point", "description": "Literally \"Point\", NOT \"MeshPoint\" - confirmed against every sampled document. The $def is named MeshPoint here only to disambiguate from Curve's own \"point\" (int, an index) and CurvePoint's own \"point\" field." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "position": { "$ref": "channel.schema.json#/$defs/Vec2Channel", "description": "The point's position, in the layer's own local coordinate space (2 Moho units span the canvas height - see docs/moho-project-file-format.md §4)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4390 in MeshPoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "width": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Per-point stroke-width multiplier. Normally 1.0; a shape whose points do NOT all share one width renders as a tapered (filled-outline) stroke instead of a plain <path stroke>. See §7.6-7.7." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4390 in MeshPoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "curves": {
          "type": "array",
          "description": "The curves that pass through this point, as {curve, curve_points} pairs (NOT a plain array of curve indices, despite the field name). This SPECIFIC field is not read by moho2svg.py - the reverse mapping is rebuilt from Mesh.curves instead. Its own x-moho-disposition below is MODELLED anyway: the registry is keyed by property NAME, not by path (see check_field_coverage.load_registry), and the name `curves` is genuinely read for Mesh's own top-level curves array one level up. Flat-by-name collision, diagnosed and waived - see x-moho-contradiction-waived.",
          "items": {
            "type": "object",
            "properties": {
              "curve": { "type": "integer", "description": "Index into mesh.curves." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4435 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
              "curve_points": { "type": "integer", "description": "Index into that curve's own points array. Present on 194,901 sites/76 documents. Genuinely unread - MeshPoint._build extracts only position/width/parent (see this `curves` array's own sibling `curve` field, which carries the same 'flat-name collision, not really this owner' caveat). Probed on Bandit.mohoproj's own 403 sites, forced to 999 (an out-of-range index): inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: curve_points=999, 403 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py."}
            },
            "additionalProperties": true
          }
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4527 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py) -- NOTE: this evidence is for the KEY NAME `curves`, genuinely read as Mesh's own top-level array, not for this MeshPoint occurrence; see x-moho-contradiction-waived", "x-moho-contradiction-waived": "MeshPoint._build reads only position/width/parent (moho2svg.py:4388-4390) - it never touches this array. The disposition is kept MODELLED only because the registry is flat by key name and the same name `curves` genuinely is read one level up as Mesh's own top-level array (see docs/moho-field-coverage-plan.md Task 8 fix round 1, Finding A)."},
        "parent": { "type": "integer", "description": "Point-level bone binding (\"link points to bone\"): a bone index this point follows rigidly, overriding its layer's own binding. -2/-1 both mean \"use the layer's binding\"; a real bone index appears on ~4,000 points across 119 meshes in the sample set. Read unconditionally into the model (MeshPoint._build) and APPLIED by Exporter._deformed_point_mapper when RenderSettings.point_bone_binding is set (--point-bones, off by default) - see the module docstring's 2026-08 re-measurement. CORRECTS an earlier revision of this schema, which wrongly claimed this field is unconsumed by moho2svg.py." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4391 in MeshPoint._build, applied by Exporter._deformed_point_mapper behind --point-bones (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "colored": { "type": "boolean", "description": "Whether per-point vertex colour (the `color` channel) is active. **Corrected (M1.4a):** an earlier revision of this description claimed \"false on every sampled point\" - a 76-document corpus scan found `colored` true on 156 of 182,522 occurrences, concentrated in the Snow-girl/Snow-girl-cut* documents (e.g. 27 true points in Snow-girl-cut10.mohoproj). Forcing it false on that document's own true sites (a real change from the stored value) AFFECTS RENDER at frame 0 - so `color`/`color_strength` are NOT dead weight once a document actually uses vertex colouring, contrary to the untested guess this description used to make. A separate probe forcing it TRUE on Bandit.mohoproj (which has zero true sites of its own - a weaker, non-representative negative, superseded by the Snow-girl-cut10 result above) was inert there." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: colored=false, 4785 sites, Snow-girl-cut10.mohoproj frame 0, AFFECTS RENDER. Not read by moho2svg.py/moho2lottie.py (MeshPoint._build extracts only position/width/parent).", "x-moho-render": "pending"},
        "color": { "$ref": "channel.schema.json#/$defs/ColorChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7273 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "color_strength": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "A per-point vertex-colour blend strength, paired with `colored`/`color` above. Observed non-constant across the corpus (76-document scan of every stored `val`): `1.0` on 182,447 of 182,554 stored keyframe values (182,522 sites - some animated with more than one keyframe, hence more values than sites), but genuinely different values on the rest - `1.061314` (46), `1.157664` (23), `0.5` (20), `0.2` (8) and four smaller counts, 9 distinct values total. Forcing it to `0.3` on every one of Bandit.mohoproj's 396 occurrences (all stored 1.0, so a real change) left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: color_strength ValChannel val 0.3, 396 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py."},
        "color_drift": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Observed only in newer-generation (version 1045-era) documents - present in 10 of 76 corpus documents, 15,173 occurrences, constant 0.0 in all of them. Forcing it to 0.5 on every one of Bandit.mohoproj's 396 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: color_drift ValChannel val 0.5, 396 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py."},
        "opacity": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Per-point opacity, observed only in newer-generation documents - present in the same 10 of 76 corpus documents as color_drift above, 15,173 occurrences, constant 1.0 in all of them. Distinct from the SVG `opacity` presentation attribute svg2moho.py reads on input elements (a name collision only - that attribute feeds fill/stroke alpha math, not this field). Forcing it to 0.3 on every one of Bandit.mohoproj's 396 occurrences (all stored 1.0) left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: opacity ValChannel val 0.3, 396 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py under this name (MeshPoint._build extracts only position/width/parent; no `.get(\"opacity\")` on a Point dict anywhere)."},
        "selected": { "type": "boolean", "description": "Editor state." , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."}
      },
      "additionalProperties": true
    },

    "Curve": {
      "type": "object",
      "required": ["type", "closed", "points"],
      "properties": {
        "type": { "const": "Curve" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "closed": { "type": "boolean", "description": "Closed: one segment per point, last wraps to first. Open: one fewer segment than points." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4356 in Curve._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "points": { "type": "array", "items": { "$ref": "#/$defs/CurvePoint" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4526 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "num_points": { "type": "integer", "description": "Redundant with len(points). Present on 26,771 sites/76 documents. Probed on Bandit.mohoproj's own 114 sites, forced to 999 (mismatching the real points array length): inert - Moho's own loader tolerates the mismatch and renders unaffected, consistent with this being a cached/redundant count rather than something re-validated at render time." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: num_points=999, 114 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py."},
        "start_percent": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Moho's \"Stroke Exposure\" tool (AS 7.0, channel id CHANNEL_CURVEEXP): trims the drawn start of the curve's OUTLINE as a fraction of ARC LENGTH, leaving any fill whole. DECODED AND APPLIED (Exporter._stroke_trims, corrected from an earlier revision that called this unread) - confirmed against Moho's own render on a purpose-built TransformBoneTool.animeproj variant (end_percent forced to 0.5/0.75 on a filled+outlined closed curve: the fill stayed whole in both, the outline's tail was dropped). Observed constant -0.1 (Moho's untrimmed sentinel, clamped into [0,1]) in every naturally-occurring sample; the one corpus document that DOES trim (FoxAndGhost.animeproj, 24 curves) draws those layers nowhere reachable, so this decode rests on the purpose-built probe, not corpus reach." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4357 in Curve._build, applied by Exporter._stroke_trims (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "end_percent": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "As start_percent, trimming the outline's end. Observed constant 1.1 (Moho's untrimmed sentinel) in every naturally-occurring sample." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4357 in Curve._build, applied by Exporter._stroke_trims (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "profile_layer_uuid": { "type": "string", "description": "A \"curve profile\" that repeats another curve's shape along this one. **Corrected (M1.4a):** an earlier revision of this description claimed \"empty in every sample\" - a 76-document corpus scan found a real, non-empty UUID (`10f7ac6a-4ae9-4b7b-81d7-54f1d97a55c0`) on 14 of 26,771 occurrences, all in Gathered-01Intro2.mohoproj and Gathered-02Wire2.mohoproj (7 each) - the feature genuinely is used, just rarely. Clearing it to `\"\"` on every one of Gathered-01Intro2.mohoproj's 940 curves (22 of them carrying that UUID) AFFECTS RENDER at frame 0." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: profile_layer_uuid=\"\", 940 sites, Gathered-01Intro2.mohoproj frame 0, AFFECTS RENDER. Not read by moho2svg.py/moho2lottie.py (Curve._build extracts only closed/points/start_percent/end_percent).", "x-moho-render": "pending"},
        "profile_curve_id": { "type": "integer", "description": "Index into the curve profile named by profile_layer_uuid. **Corrected (M1.4a):** an earlier revision of this description claimed \"constant -1 (unset) in every sample\" - a 76-document corpus scan found `0` on 44 of 26,771 occurrences, in the same two documents (Gathered-01Intro2.mohoproj, Gathered-02Wire2.mohoproj, 22 each) where profile_layer_uuid is genuinely non-empty - so `0` is a real index, not just a second sentinel. Forcing it to -1 (the corpus's own \"unset\" value elsewhere) on every one of Gathered-01Intro2.mohoproj's 940 curves AFFECTS RENDER at frame 0." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: profile_curve_id=-1, 940 sites, Gathered-01Intro2.mohoproj frame 0, AFFECTS RENDER. Not read by moho2svg.py/moho2lottie.py.", "x-moho-render": "pending"},
        "profile_repeat": { "type": "integer", "description": "How many times the named profile repeats along this curve. A 76-document corpus scan found this constant at 16 across all 26,771 occurrences, in EVERY document - including the two (Gathered-01Intro2.mohoproj, Gathered-02Wire2.mohoproj) where the profile feature is genuinely active (profile_layer_uuid non-empty), not only where it is unset as an earlier revision of this description implied. Forcing it to 4 on every one of Gathered-01Intro2.mohoproj's 940 curves AFFECTS RENDER at frame 0 - so the constant value is corpus convention, not evidence the field is inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: profile_repeat=4, 940 sites, Gathered-01Intro2.mohoproj frame 0, AFFECTS RENDER. Not read by moho2svg.py/moho2lottie.py.", "x-moho-render": "pending"},
        "profile_offset": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "An offset along the named profile. Present in the same 10 \"newer-generation\" corpus documents as MeshPoint.color_drift/opacity above (Bandit.mohoproj is one, 5,309 occurrences), constant 0.0 in every one - notably, this is a DIFFERENT set of documents from the two where profile_layer_uuid/profile_curve_id are genuinely active (Gathered-01Intro2.mohoproj/Gathered-02Wire2.mohoproj carry neither profile_offset nor color_drift/opacity at all), so no corpus document has both an active profile AND a non-zero-capable profile_offset site to probe together. Forcing it to 0.5 on every one of Bandit.mohoproj's 114 occurrences (feature inactive there - profile_layer_uuid empty throughout) left frame 25 byte-identical; a real effect while the profile feature is genuinely active is not ruled out, the same residual-precondition risk as `transforms.following`/`follow_curve` elsewhere in this schema." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: profile_offset ValChannel val 0.5, 114 sites, Bandit.mohoproj frame 25, inert. Not read by moho2svg.py/moho2lottie.py."}
      },
      "additionalProperties": true
    },

    "CurvePoint": {
      "type": "object",
      "description": "One vertex of a Curve, referencing a MeshPoint by index plus the curvature data used to reconstruct explicit Bezier handles (see docs/moho-project-file-format.md §7.5: handle length = distance_to_neighbour * smoothness * weight, confirmed exact against 209 reference handles; handle direction is a chord-length-weighted blend of the two neighbouring chords, NOT a simple normalize(next-prev)). weight_in/weight_out/offset_in/offset_out are OPTIONAL - an older sampled document (format version 1021) carries only point/smoothness/segments_on on every curve point, with no weight or offset fields at all (presumably a simpler, symmetric-handle-only curve representation predating the asymmetric weight/offset feature).",
      "required": ["point", "smoothness", "segments_on"],
      "properties": {
        "point": { "type": "integer", "description": "Index into the mesh's `points` array." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4321 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "smoothness": {
          "$ref": "channel.schema.json#/$defs/ValChannel",
          "description": "Curvature; 0 = sharp corner (handles collapse onto the point).",
          "x-moho-disposition": "MODELLED",
          "x-moho-evidence": "moho2svg.py BezierReconstructor; handle length = distance_to_neighbour * smoothness * weight, confirmed exact against 209 reference handles (docs/moho-project-file-format.md §7.5)"
        },
        "weight_in": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "How far the incoming handle reaches toward its neighbour, as a fraction of the distance to it." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4323 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "weight_out": { "$ref": "channel.schema.json#/$defs/ValChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4324 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "offset_in": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "A small rotation in radians, producing asymmetric curves." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4325 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "offset_out": { "$ref": "channel.schema.json#/$defs/ValChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4326 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "segments_on": {
          "type": "boolean",
          "description": "Whether the segment LEAVING this point is drawn at all. false creates a real, deliberate gap in the outline (confirmed: an umbrella panel that only shows part of its boundary as a drawn line). For a combo_mode==3 (intersect) shape specifically, a false segment can instead mark a piece of curve Moho's own boolean solver replaced with a computed crossing edge - moho2svg.py handles that case by NOT dropping the segment and relying on the intersect-clip to cut it correctly (see Shape.combo_mode below and the module docstring's BOOLEAN SHAPE COMBINATIONS section)."
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4327 in CurvePoint._build (trace: out/traced_keys.json, see tools/trace_fields.py)"}
      },
      "additionalProperties": true
    },

    "Edges": {
      "type": "object",
      "description": "A shape's boundary as an UNORDERED set of curve segments - three parallel arrays, always equal length. NOT reliably a walk in list order, and `flag` is NOT a reliable direction bit (real files exist where segment order is strictly descending with flag 0 throughout). Must be re-traced as an undirected graph keyed by rounded endpoint coordinates - see docs/moho-project-file-format.md §7.9 and the module docstring's PathTracer.",
      "required": ["curve", "segment", "flag"],
      "properties": {
        "curve": { "type": "array", "items": { "type": "integer" }, "description": "Index into mesh.curves for each edge." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4435 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "segment": { "type": "array", "items": { "type": "integer" }, "description": "Index into that curve's segment list (segment i runs from curve_point[i] to curve_point[i+1])." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4435 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "flag": { "type": "array", "items": { "type": "integer" }, "description": "Observed 0 (majority) or 1. NOT a reliable direction bit - do not trust it for path-walk order." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4435 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"}
      },
      "additionalProperties": true
    },

    "Shape": {
      "type": "object",
      "description": "One filled/stroked region of a mesh. See docs/moho-project-file-format.md §7.4/§7.8 and the module docstring's BOOLEAN SHAPE COMBINATIONS section.",
      "required": ["type", "id", "has_fill", "has_outline", "edges", "style"],
      "properties": {
        "type": { "const": "Shape" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "id": { "type": "integer", "description": "Shape identity, referenced by mesh.shape_order (an id registry, not a z-order)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4437 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "name": { "type": "string", "description": "Editor label, e.g. \"S1\", or \"\" (empty is a real value Moho itself writes, e.g. for a TextLayer glyph's synthesised shapes - distinct from the key being entirely absent)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2681 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "has_fill": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4443 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "has_outline": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4444 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "combo_mode": {
          "type": "integer",
          "description": "How this shape combines with the shape(s) immediately before it in the same layer. 0 = normal (starts a new independent group). 1 = union (merged into the current group; the shared boundary disappears; the combined outline is stroked using the group's FIRST/base member's own styling, not its own - confirmed: a union member's own line_width is absent from Moho's export). 3 = intersect (clipped to the union of the group's solid (0 or 1) members so far - moho2svg.py approximates this via SVG masking, not a true geometric path intersection, which can leave a gap in a member's own outline where Moho's real boolean solver would substitute a computed crossing edge - fixed for the plain-stroke case by not dropping segments_on==false segments on an intersect member and letting the clip cut them). 2 has been reported in real files but its effect has not been reverse-engineered (absent from every document censused so far). ABSENT from the shape entirely in older-generation (version 1038-era) documents - treat a missing combo_mode as 0.",
          "enum": [0, 1, 2, 3]
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4445 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "edges": { "$ref": "#/$defs/Edges" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4434 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "style": { "$ref": "style.schema.json#/$defs/Style", "description": "The shape's own style values (same shape as a top-level named Style, see style.schema.json). May be fully populated (newer-generation documents) or left with every define_* flag false and real values kept in a document-wide named style referenced by inherited_style_uuid/name (older-generation documents) - see docs/moho-project-file-format.md §8.2." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2605 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "inherited_style_uuid": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2609 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "inherited_style_name": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2609 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "inherited_style2_uuid": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2609 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "inherited_style2_name": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2609 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "effect_scale": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Gradient scale (fill_style placement), default 1.0." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4448 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "effect_rotation": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Gradient rotation in radians, default 0.0. Only affects a LINEAR gradient - a radial gradient ignores rotation entirely (see docs/moho-export-pipeline.md §7.4)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4449 in Shape._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "effect_offset": { "$ref": "channel.schema.json#/$defs/Vec2Channel", "description": "A gradient centre offset. **Corrected (M1.3):** an earlier revision of this description claimed \"nothing observed supplies a non-zero value\" - a direct scan of all 76 corpus documents contradicts that: 30 of the 76 documents carry a non-zero effect_offset somewhere, 276 of 22,144 total occurrences, with the corpus-wide maximum {x: 0.020666, y: 1.386204} in Snow-girl/Snow-girl-cut10.mohoproj (a real animated Vec2 channel keyframe, not this milestone's own probed document). Confirmed to move pixels: forcing every occurrence in SketchBone.animeproj (a smaller, non-maximal example, but one with real gradient fills to move) to {x: 0.3, y: 0.3} changed the frame-0 render (this shape's own extraction in moho2svg.py's Shape class drops this field entirely, per the docstring next to Shape.__init__ - it is written but never read back)." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: effect_offset Vec2Channel val {x:0.3,y:0.3}, 190 sites, SketchBone.animeproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "fill_allowed": { "type": "boolean", "description": "Presumably \"this shape may be filled at all\", distinct from has_fill. Interaction with has_fill is undecoded. Forcing it false on every one of Bandit.mohoproj's 112 occurrences (a mix of true/false stored values, so not a no-op) left frame 25 byte-identical, including on shapes stored with has_fill=true - the guessed gating relationship with has_fill is not confirmed by this probe, at least not in a way that shows up at this frame." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: fill_allowed=false, 112 sites, Bandit.mohoproj frame 25, inert"},
        "combo_blend_anim": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Presumably animates a soft boolean-combination blend. Observed only in newer-generation documents, always 0.0. Forcing it to 0.7 on every one of Bandit.mohoproj's 112 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: combo_blend_anim ValChannel val 0.7, 112 sites, Bandit.mohoproj frame 25, inert"},
        "3d_thickness": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Observed constant 0.125 in every sample." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "not yet investigated"},
        "selected": { "type": "boolean", "description": "Editor state." , "x-moho-disposition": "PRESERVE", "x-moho-evidence": "Editor/view/provenance state matched by DESCRIPTION_PATTERNS; excluded from the content metric but still required to survive an unedited load->save (tools/check_roundtrip.py)."}
      },
      "additionalProperties": true
    },

    "PointGroup": {
      "type": "object",
      "required": ["type", "name", "points"],
      "properties": {
        "type": { "const": "PointGroup" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "name": { "type": "string" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2681 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "points": { "type": "array", "items": { "type": "integer" } , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:4526 in Mesh._build (trace: out/traced_keys.json, see tools/trace_fields.py)"}
      },
      "additionalProperties": true
    }
  }
}
