{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moho2svg.local/schema/style.schema.json",
  "title": "Moho style (named style and a shape's own inline style)",
  "description": "See docs/moho-project-file-format.md §8 for the full inheritance-resolution rule and the two-generation (version 1038 vs 1045) usage split. IMPORTANT: `line_width`, `line_caps`, and every `brush_*` field are PLAIN values, never channels - confirmed across every sampled style (Moho does not animate them).",
  "$defs": {

    "Style": {
      "type": "object",
      "description": "The exact same object shape is used both for doc.styles[] (a document-wide named style) and for shape.style (a shape's own, usually-partial style). A shape's own style additionally carries the inherited_style*_uuid/name fields shown on Shape (mesh.schema.json) - both locations (on the shape itself, or inside the shape's own style object) are observed in real files and both must be checked.",
      "required": ["type"],
      "properties": {
        "type": { "const": "Style" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "name": { "type": "string", "description": "Empty on a shape's own inline style; a real name (e.g. \"yanak\") on a document-wide named style." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2681 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "uuid": { "type": "string", "description": "Lookup key used by Shape.inherited_style_uuid / inherited_style2_uuid." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2679 in StyleTable.build (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "define_fill_color": { "type": "boolean", "description": "If true on a NAMED style and false on the shape's own style, the named style's fill_color overrides the shape's own - see the resolution rule in docs/moho-project-file-format.md §8.2." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2616 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "define_line_col": { "type": "boolean" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2616 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "define_line_width": { "type": "boolean", "description": "Also gates whether line_caps and the brush_* fields are inherited from the named style (they ride on this same flag, since a brush only ever styles the line)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2616 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "fill_color": { "$ref": "channel.schema.json#/$defs/ColorChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2617 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "line_color": { "$ref": "channel.schema.json#/$defs/ColorChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2617 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "line_width": { "type": "number", "description": "PLAIN float (not a channel) - Moho does not animate line width. Observed as a handful of quantised values per document (e.g. 0.0028-0.092)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2617 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "line_caps": { "type": "integer", "enum": [0, 1, 2], "description": "0 = butt, 1 = round, 2 = square. Only 1 (round) observed across the entire sample set, but 0/2 are named in moho2svg.py's own LINE_CAP_NAMES mapping." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2638 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},

        "fill_style": {
          "oneOf": [
            { "$ref": "#/$defs/Gradient" },
            { "$ref": "#/$defs/Crayon" },
            { "$ref": "#/$defs/Halo" },
            { "$ref": "#/$defs/Shaded" },
            { "$ref": "#/$defs/SoftStyle" }
          ],
          "description": "A styled fill effect. Present BOTH on a named style AND inline on a shape's own style object; neither is rare (1160 named-style vs 652 inline across the 46-file sample set). Five variants observed as a fill: SS_Gradient2 (1401), SS_Halo (198), SS_Shaded (96), SS_Soft (91) and SS_Crayon (26). moho2svg.py only renders SS_Gradient2; every other variant emits a per-shape stderr warning and falls back to the shape's flat fill_color." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2632 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "fill_style_id": { "type": "integer", "enum": [0, 2, 4, 9, 10, 11, 12], "description": "The effect KIND, as an integer parallel to the effect object's own `type` string: 0 = SS_Shaded, 2 = SS_Soft, 4 = SS_Halo, 9 = SS_Gradient2, 10 = SS_Texture2, 11 = SS_Shadow, 12 = SS_Crayon. Confirmed to agree with `type` in all 2003 effect instances across the sample set, with no exceptions - so it never carries independent information in any sampled document. But it is NOT redundant to Moho's own renderer, contrary to an earlier revision of this description: forcing every occurrence in SketchBone.animeproj (all stored as 9, matching the shape's real `SS_Gradient2` fill effect) to 4 (SS_Halo) - leaving `type` itself and every other style field untouched - changed the frame-0 render. Moho reads this integer separately from `type` to decide how to draw the effect, at least in some circumstances; which one wins when they disagree in a real (non-probe) document is not established, since no sampled document ever has them disagree naturally. The enum lists every kind seen in ANY of the three slots, not just this one." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: fill_style_id=4, 76 sites, SketchBone.animeproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "line_style": {
          "oneOf": [
            { "$ref": "#/$defs/Gradient" },
            { "$ref": "#/$defs/SoftStyle" },
            { "$ref": "#/$defs/Shaded" },
            { "$ref": "#/$defs/ShadowStyle" }
          ],
          "description": "A styled STROKE effect rather than a fill. Four variants observed as a line: SS_Gradient2 (116 occurrences across 16 files), SS_Soft (40), SS_Shaded (18) and SS_Shadow (3). Unlike fill_style, every one of these sits on a NAMED style; none is inline on a shape. moho2svg.py reads this slot and renders SS_Gradient2 as a real paint server on the outline (`stroke` for a uniform-width outline, `fill` for a tapered one); the other three warn and draw a flat stroke. One caveat worth knowing: a gradient outline on a BRUSH stroke is also warned and drawn flat, because a brush tints image pixels rather than painting with a paint server - and all 6 drawn gradient outlines in the sample set are brush strokes." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2636 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "line_style_id": { "type": "integer", "enum": [0, 2, 4, 9, 10, 11, 12], "description": "The effect kind - same enum and same confirmation as fill_style_id. Present on 311 sites/39 documents. M1.5 batch 7: probed the same way as fill_style_id (Snow_wars/01 opening.moho's own 9 real sites, forced from their stored kind to 4/SS_Halo, leaving `type` and every other style field untouched): **AFFECTS RENDER** - Moho reads this integer separately from `type` for the line/outline slot too, consistent with fill_style_id's own finding." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: line_style_id=4, 9 sites, Snow_wars/01 opening.moho frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "fill_style2": {
          "oneOf": [
            { "$ref": "#/$defs/Texture2" },
            { "$ref": "#/$defs/SoftStyle" },
            { "$ref": "#/$defs/Gradient" }
          ],
          "description": "A SECOND fill effect slot, layered on top of `fill_style`. Three variants observed: SS_Texture2 (12 occurrences across 3 files), SS_Soft (2, in DarkMan.mohoproj), and SS_Gradient2 - a 76-document-census finding (Snow_wars/01 opening.moho, inside a `.moho` archive, absent from the earlier 46-bare-file sample). Not rendered by moho2svg.py, which warns per shape and exports without the second effect."
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2634 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "fill_style2_id": { "type": "integer", "enum": [0, 2, 4, 9, 10, 11, 12], "description": "The effect kind - same enum and same confirmation as fill_style_id. Present on 32 sites/6 documents (Snow_wars/01 opening.moho, Snow_wars/15 weaving.moho, DarkMan.mohoproj, Others/IndependentAngle.animeproj, Others/MaximumIKStrethching.animeproj, Others/TargetBone.animeproj). M1.5 batch 7: probed the same way as fill_style_id (Others/IndependentAngle.animeproj's own 4 real SS_Texture2 sites, forced from their stored kind to 9/SS_Gradient2, leaving `type` and every other style field untouched): **AFFECTS RENDER** - Moho reads this integer separately from `type` for the SECOND fill slot too, consistent with fill_style_id's own finding." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: fill_style2_id=9, 4 sites, Others/IndependentAngle.animeproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},

        "brush_name": { "type": "string", "description": "Identifies a brush texture asset by name, possibly with an old-style numeric suffix baked in (e.g. Brush567_0_20_50.png names the file Brush567.png) - see docs/moho-project-file-format.md §8.5 for the full resolution algorithm. Empty string means no brush (plain uniform stroke)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2645 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "brush_jitter": { "type": "number", "description": "Random rotation spread per dab, in RADIANS." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2648 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "brush_spacing": { "type": "number", "description": "Dab spacing, as a fraction of the dab's own diameter." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2648 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "brush_align": { "type": "boolean", "description": "Whether each dab rotates to the local path tangent." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2648 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "brush_tint": { "type": "boolean", "description": "Whether the (greyscale) texture is recoloured to line_color, or used with its own native multi-colour pixels as-is." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2648 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "brush_randomize": { "type": "boolean", "description": "Per-dab size variance. Read by moho2svg.py but not applied when rendering. Forcing it true on every one of Bandit.mohoproj's 124 occurrences (a mix of true/false stored values) left frame 25 byte-identical - a real brush stroke IS drawn there (confirmed non-empty brush_name values on the touched shapes), so this is a genuine negative on that document, not an untested no-brush frame." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_randomize=true, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_rand_order": { "type": "boolean", "description": "Read by moho2svg.py but not applied - randomOrder is instead read from the brush's own .mohobrush archive (see docs/moho-project-file-format.md §8.5). Forcing it true on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_rand_order=true, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_merged_alpha": { "type": "boolean", "description": "Whether overlapping dabs cap combined opacity instead of compounding. Read but not applied - overlapping dabs always compound via ordinary SVG alpha blending. Forcing it true on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_merged_alpha=true, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_angle_drift": { "type": "number", "description": "Read but not applied. Forcing it to 1.0 (from a mix of 0.0/0.261799 stored values) on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_angle_drift=1.0, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_random_interval": { "type": "integer", "description": "Observed only in newer-generation documents, always 1. Forcing it to 2 on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_random_interval=2, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_size_amp": { "type": "number", "description": "Observed only in newer-generation documents. Forcing it to 0.9 (and separately to 0.95 with brush_randomize=true as an explicit precondition, in case the per-dab variance this field presumably scales is gated behind that switch) on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical both times." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_size_amp=0.9, 124 sites, Bandit.mohoproj frame 25, inert; and brush_size_amp=0.95, 124 sites, Bandit.mohoproj frame 25, precondition brush_randomize=true, inert"},
        "brush_size_scale": { "type": "number", "description": "Observed only in newer-generation documents. Forcing it to 0.9 on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_size_scale=0.9, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_hue_drift": { "type": "number", "description": "Observed only in newer-generation documents, always 0.0. Forcing it to 0.5 on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_hue_drift=0.5, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_sat_drift": { "type": "number", "description": "Forcing it to 0.5 (from a constant 0.0) on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_sat_drift=0.5, 124 sites, Bandit.mohoproj frame 25, inert"},
        "brush_val_drift": { "type": "number", "description": "Forcing it to 0.5 (from a constant 0.0) on every one of Bandit.mohoproj's 124 occurrences left frame 25 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: brush_val_drift=0.5, 124 sites, Bandit.mohoproj frame 25, inert"}
      },
      "additionalProperties": true
    },

    "Gradient": {
      "type": "object",
      "description": "A gradient fill (or, rarely, stroke) specification. Placement (centre/radius) as computed by moho2svg.py is approximate, not pixel-matched to Moho's own differently-parameterised placement - see docs/moho-project-file-format.md §8.3.",
      "required": ["type", "gradient_type", "gradients"],
      "properties": {
        "type": { "const": "SS_Gradient2", "description": "Only this literal type is understood; any other value is rejected by moho2svg.py with a warning (and the flat fill colour is used instead)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "gradient_type": { "type": "integer", "enum": [0, 1, 2], "description": "0 = linear, 1 = radial. 2 is a 76-document-census finding (Snow_wars/01 opening.moho, only reachable by opening the `.moho` archives - absent from the earlier 46-bare-file sample, which is why an earlier revision of this schema declared the enum as just [0, 1] and rejected this real document). moho2svg.py only branches on `== 1`; every other value, this one included, falls through to the linear path - not confirmed correct for gradient_type 2, only confirmed not to crash." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7283 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "through_alpha": { "type": "boolean", "description": "Not read by moho2svg.py. Declared on both Gradient and Texture2 (see that $def's own occurrence of the same flat name) - genuinely a shared name across two owners in real files, confirmed by a 76-document corpus scan: 37 documents carry this key, split between `SS_Gradient2` (this owner) and `SS_Texture2` sites. But only THIS owner has a writer - svg2moho.py:847/lottie2moho.py:1039 both build an `SS_Gradient2` dict (the same object shape in both files, one code path per exporter) and always set it to `false`; neither exporter ever emits an `SS_Texture2` object at all (grep confirms zero occurrences of the string \"Texture2\" in either file), so Texture2.through_alpha has nothing to register here and stays UNKNOWN pending M2's observe-then-probe recipe - see that $def's own entry. Probed on SketchBone.animeproj, where all 83 occurrences are this (Gradient) owner, stored `false`: forcing every one to `true` left frame 0 byte-identical." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: through_alpha=true, 83 sites, SketchBone.animeproj frame 0, inert", "x-moho-registry-conflict-waived": "Flat-by-name collision with Texture2.through_alpha (this same file) - genuinely a different owner, hand-confirmed by a 76-document corpus scan (both `SS_Gradient2` and `SS_Texture2` sites exist for this key name). This (Gradient) owner IS writer-backed (svg2moho.py:847, lottie2moho.py:1039) and probed EDITABLE; Texture2's owner has no writer in either exporter (grep-confirmed) and stays UNKNOWN. check_field_coverage.py's effective_disposition() scores the flat corpus key `through_alpha` as covered via this occurrence, which is correct: at least one real owner of this name is writer-backed and measured."},
        "gradients": {
          "type": "array",
          "description": "Gradient stops, each with its own animatable position and colour.",
          "items": {
            "type": "object",
            "required": ["location", "color"],
            "properties": {
              "location": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Stop position, 0.0-1.0." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7272 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"},
              "color": { "$ref": "channel.schema.json#/$defs/ColorChannel" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7273 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"}
            },
            "additionalProperties": true
          }
        , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7271 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"}
      },
      "additionalProperties": true
    },

    "Crayon": {
      "type": "object",
      "description": "A textured, crayon-like fill effect. Not decoded - not read by moho2svg.py, which falls back to the shape's flat fill_color.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Crayon" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "line_width": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Despite the name, this styles the crayon texture's own stroke-like density, not the shape's line_width." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2617 in ResolvedStyle.resolve (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "density": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual ch. 13 (Style Window, Crayon): \"Decrease the setting to give the crayon a more grainy appearance. Increase the setting to make the crayon more 'solid.'\" Shares its flat name with the completely unrelated `Physics.density` (rigid-body mass density, `layer.schema.json`'s own shared `$defs/Physics`) - confirmed a genuine two-owner collision, not a shared code path (through_alpha/M1.3 precedent): see `Physics.density`'s own entry for its corpus occurrences and separately-probed AFFECTS RENDER result. THIS owner is real but rare: 26 occurrences across 8 documents (`OffsetBoneTool.animeproj`, 19 sites at `1.0`; 7 `Snow-girl/Snow-girl-cut*.mohoproj` files at `0.8`), all as an INLINE `fill_style` on individual shapes rather than a named style, matching this format's `SS_Gradient2` finding that a shape effect need not live on a named style ([`docs/moho-project-file-format.md` § 8.3](../docs/moho-project-file-format.md#83-style-effect-variants-46-file-finding)). Isolated from `Physics.density` (a hand-edited copy of `OffsetBoneTool.animeproj` with that document's 25 `Physics.density` sites stripped first) and forced from `1.0` to `0.01` (an extreme, 100x swing) at frame 60: inert, on both this value and a milder `0.2` try. Not fully resolved: the same document's own `line_width` (this $def's sibling field, which actually shapes the crayon texture's stroke-like thickness, not `density`'s own graininess) sits at `0.001852` on the very shapes probed - a near-zero stroke width plausibly leaves too little crayon texture on screen for its OWN density/graininess to visibly change, but a precondition forcing `line_width` up first was not completed (that field is itself declared under several OTHER same-named owners across this schema - `Style.line_width`, `TextLayer.linewidth`, etc. - and would need the same owner-isolation treatment `velocity`/`density` themselves needed, which this batch's scope did not extend to)." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: density Val channel val 0.01 (and separately 0.2), 19 sites, OffsetBoneTool.animeproj (Physics.density's 25 sites stripped first to isolate this owner) frame 60, inert"},
        "clear_background": { "type": "boolean", "description": "Manual ch. 13 (Style Window, Crayon) does not name this checkbox directly, but the Crayon panel's own \"Clear Background\" option presumably lets the crayon texture paint without the shape's own flat fill showing through underneath. Constant false on all 26 corpus occurrences (8 documents). M1.5 batch 5: forced true on Snow-girl-cut12.mohoproj's own 1 site (frame 0, a genuinely Crayon-styled, visibly-drawn shape - not Bandit.mohoproj, which has no Crayon usage at all): inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: clear_background true, 1 site, Snow-girl-cut12.mohoproj frame 0, inert"},
        "reduce_randomization": { "type": "boolean", "description": "Manual: presumably dampens the crayon texture's own per-dab jitter/randomization (paired with `rand_seed` below). Constant true on all 26 corpus occurrences (8 documents). M1.5 batch 5: forced false on the same Snow-girl-cut12.mohoproj site as `clear_background`: inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: reduce_randomization false, 1 site, Snow-girl-cut12.mohoproj frame 0, inert"},
        "rand_seed": { "type": "integer", "description": "The crayon texture's own per-shape random seed (2 distinct corpus values: 1741667360 on OffsetBoneTool.animeproj's 19 sites, 4634 on 7 Snow-girl-cut*.mohoproj files' sites). M1.5 batch 5: forced to 999999 on the same Snow-girl-cut12.mohoproj site as `clear_background`/`reduce_randomization`: inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: rand_seed 999999, 1 site, Snow-girl-cut12.mohoproj frame 0, inert"}
      },
      "additionalProperties": true
    },

    "SoftStyle": {
      "type": "object",
      "description": "The \"Soft Edge\" effect (manual ch. 13.02): blurs the edge of whatever it styles. Usable in all three slots, and most often a FILL - 91 fill_style vs 40 line_style vs 2 fill_style2 occurrences (an earlier revision of this schema described it as line-only, before the sample set grew). Not rendered by moho2svg.py, which warns per shape and draws the plain colour underneath.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Soft" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "blur_radius": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Blur Radius\" - how soft the edge is. One of TWO schema owners of this flat name (this file's own Halo, a completely different fill effect - see that $def's own `blur_radius` entry). M1.5 batch 5: isolated on Snow-girl-cut12.mohoproj (a document with real, active occurrences of both Halo and SoftStyle) by deleting Halo's own `blur_radius` sites first, forcing the remaining 6 SoftStyle-only sites from their stored values to 0.05 (Val channel) at frame 0: inert - unlike Halo.blur_radius (AFFECTS RENDER on the same document/frame with the roles reversed), matching moho2svg.py's own note that SS_Soft is not rendered by this exporter and suggesting Moho's own soft-edge blur may need a larger radius, an animated frame, or a shape geometry this fixture's 6 SoftStyle sites do not exercise to become visible - recorded as a real, non-confounded negative under this document/value, not a claim that no SoftStyle.blur_radius value ever matters." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: blur_radius Val channel val 0.05, 6 sites (isolated from Halo's own occurrences), Snow-girl-cut12.mohoproj frame 0, inert"},
        "threshold": { "type": "boolean", "description": "Observed as a plain boolean here (not wrapped in a channel), unlike LayerShadow/LayerShading/PerspectiveShadow's own threshold field which IS a BoolChannel - confirmed by direct inspection (TargetBone.animeproj). Manual: applied after the blur, to smooth corners." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "M1.4b: SoftStyle is a shape-level style effect (SS_Soft) - a completely different subsystem from the layer-level LayerEffects/LayerShadow/LayerShading/PerspectiveShadow (all four writer-backed by lottie2moho.py and now EDITABLE - see layer.schema.json's LayerEffects.threshold). Neither svg2moho.py nor lottie2moho.py ever synthesizes an SS_Soft object, so this owner has no writer-backed value to probe; needs M2's observe-then-probe recipe instead."}
      },
      "additionalProperties": true
    },

    "Halo": {
      "type": "object",
      "description": "The \"Halo\" fill effect (manual ch. 13.02): a coloured, blurred rim inside the edge of the fill. Observed only as a fill_style - 198 occurrences across 10 files, which makes it the most-used effect in the sample set after SS_Gradient2. Not rendered by moho2svg.py, which warns per shape and draws the flat fill_color instead. Field names below are matched to the manual's own dialog labels.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Halo" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "halo_radius": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Inset Radius\" - the radius of the inner area. In Moho space units (observed 0.002778-0.013889, 3 distinct corpus values across 198 sites/10 documents). M1.5 batch 5: forced to a Val channel of 0.05 on all 9 sites of Snow-girl-cut12.mohoproj (real, actively-drawn Halo shapes) at frame 0: AFFECTS RENDER (the first attempt used a bare scalar `0.05` in place of the channel object and corrupted the document - Moho reported \"Unable to load document (corrupt)\" - see this batch's own report for why every ValChannel/BoolChannel-typed field in this batch is probed with the full `{type,ref,mute,when,val,interp}` shape, not a bare number, even though a bare BOOLEAN or bare COLOR dict was separately confirmed tolerated for `halo_only`/`halo_color` below)." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: halo_radius Val channel val 0.05, 9 sites, Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "blur_radius": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Blur Radius\" - the softness around the halo. One of TWO schema owners of this flat name (style.schema.json's own SoftStyle, a completely different soft-edge effect - see that $def's own `blur_radius` entry). M1.5 batch 5: isolated on Snow-girl-cut12.mohoproj by deleting SoftStyle's own `blur_radius` sites first, forcing the remaining 9 Halo-only sites from their stored 0.003-0.014 range to 0.05 (Val channel) at frame 0: AFFECTS RENDER." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: blur_radius Val channel val 0.05, 9 sites (isolated from SoftStyle's own occurrences), Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "halo_color": { "$ref": "channel.schema.json#/$defs/ColorChannel", "description": "Manual: \"Halo Color\". Observed 4 distinct corpus colours across 198 sites/10 documents. M1.5 batch 5: forced to opaque red `{r:1.0,g:0.0,b:0.0,a:1.0}` (a bare ColorChannel-shaped dict, not wrapped in the full `{type:\"Color\",...}` channel object - unlike `halo_radius` above, this bare shorthand rendered without corrupting the document, matching moho2svg.py's own Channel docstring note that Moho itself sometimes stores a channel field as a bare, never-animated value) on all 9 sites of Snow-girl-cut12.mohoproj at frame 0: AFFECTS RENDER." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: halo_color {r:1.0,g:0.0,b:0.0,a:1.0} (bare, not channel-wrapped), 9 sites, Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "halo_only": { "type": "boolean", "description": "Manual: \"Halo Only\" - draw the halo without the fill underneath it. Observed both false (128 sites) and true (70 sites) across the 198-site/10-document corpus. M1.5 batch 5: forced true on all 9 sites of Snow-girl-cut12.mohoproj at frame 0: AFFECTS RENDER." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: halo_only true, 9 sites, Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"}
      },
      "additionalProperties": true
    },

    "Shaded": {
      "type": "object",
      "description": "The \"Shaded\" fill effect (manual ch. 13.02): an inner drop shadow cast within the shape by a directional light. 96 fill_style and 18 line_style occurrences across 10 files. Distinct from ShadowStyle (SS_Shadow), which is a different effect with a different field set, and from the LAYER-level shadow in layer.schema.json. Not rendered by moho2svg.py, which warns per shape. Field names below are matched to the manual's own dialog labels.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Shaded" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "angle": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Light Angle\", in radians, zero at the BOTTOM of the dial (14 distinct corpus values across 775 sites/32 documents, most commonly 2.356194 = 135 degrees on 444 sites). M1.5 batch 5: one of FOUR schema owners of this flat name (this file's own ShadowStyle; layer.schema.json's LayerShading/LayerShadow, a different layer-level-compositing subsystem - see the M1.4b precedent this batch follows, `docs/moho-field-coverage-plan/m1-4b-completion-report.md`). Isolated on Snow-girl-cut12.mohoproj (a document with real, actively-styled Shaded shapes - no on/off gate needed here, unlike LayerShading/LayerShadow: presence of the SS_Shaded object in a fill_style/line_style slot IS the activation) by deleting the other three owners' `angle` sites first, forcing the remaining 2 Shaded-only sites from their stored value to a Val channel of 0.0 at frame 0: AFFECTS RENDER - the first owner of this flat name in either schema file to get a real render measurement, since M1.4b left Shaded/ShadowStyle's other fields UNKNOWN for lack of a writer (EDITABLE never needed one; it rests on tools/probe_field.py's own direct Moho-render probe)." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: angle Val channel val 0.0, 2 sites (isolated from LayerShading/LayerShadow/ShadowStyle's own occurrences), Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "offset": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Offset\" - how far the shadow is offset from the filled area, in Moho space units." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2981 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "blur": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Manual: \"Blur\" - the softness of the shadow." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "M1.4b: Shaded is a shape-level style effect (SS_Shaded) - a completely different subsystem from the layer-level LayerEffects/LayerShadow/LayerShading/PerspectiveShadow (all four writer-backed by lottie2moho.py and now EDITABLE, `.blur` in particular - see layer.schema.json's LayerEffects.blur). Neither svg2moho.py nor lottie2moho.py ever synthesizes an SS_Shaded object, so this owner has no writer-backed value to probe; needs M2's observe-then-probe recipe instead."},
        "color": { "$ref": "channel.schema.json#/$defs/ColorChannel", "description": "Manual: \"Shadow Color\" (observed black at alpha 0.07451 - these are subtle by default)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7273 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "shadow_only": { "type": "boolean", "description": "Manual: \"Shadow Only\" - draw the shadow without the fill underneath it. Constant false on all 775 corpus occurrences (32 documents) - no sampled document ever authors this on. M1.5 batch 5: forced true on Snow-girl-cut12.mohoproj's 2 real, actively-styled Shaded sites at frame 0: AFFECTS RENDER, confirming the field is read at render time even though no corpus document happens to exercise the non-default value natively." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: shadow_only true, 2 sites, Snow-girl-cut12.mohoproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "threshold": { "type": "boolean", "description": "Manual: applied after the blur, to smooth corners. A plain boolean, like SoftStyle's own." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "M1.4b: Shaded is a shape-level style effect (SS_Shaded) - a completely different subsystem from the layer-level LayerEffects/LayerShadow/LayerShading/PerspectiveShadow (all four writer-backed by lottie2moho.py and now EDITABLE - see layer.schema.json's LayerEffects.threshold). Neither svg2moho.py nor lottie2moho.py ever synthesizes an SS_Shaded object, so this owner has no writer-backed value to probe; needs M2's observe-then-probe recipe instead."}
      },
      "additionalProperties": true
    },

    "Texture2": {
      "type": "object",
      "description": "An image-texture fill, referenced from a style's `fill_style2` slot. Not decoded - not read by moho2svg.py. Note that in all 12 sampled occurrences both the `path` string and the fileref's own path are EMPTY, so no sampled document actually resolves to a texture file; the shape of a populated instance is therefore inferred from the field names, not confirmed.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Texture2" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "path": { "type": "string", "description": "Texture image path. Empty in every one of the 12 sampled occurrences (3 documents: Others/IndependentAngle.animeproj, Others/MaximumIKStrethching.animeproj, and a third sharing the same fill_style2 template - all in the `fill_style2` slot). M1.5 batch 6: the M3-shaped scoping this field carried since M1.5 (\"needs a synthesised non-empty value under a real texture-style precondition\") turned out tractable this batch, using the same isolation groundwork built for FileRef.path/relativeTo - this flat key `path` collides not only with every FileRef occurrence in the document but with the NESTED SS_Texture2FileRef.path sitting one level inside the SAME Texture2 object, so isolation pre-stamped every dict that has a sibling `relativeTo` key (i.e. every actual FileRef-shaped object, including the nested SS_Texture2FileRef) to the target value, leaving only this outer, `relativeTo`-less `path` property to vary. Forced from \"\" to a real, existing absolute image path on Others/IndependentAngle.animeproj's 4 real sites at frame 0, tried twice (once plain, once with a `fill_mode=1` precondition in case fill_mode=0 - the only value ever observed - gates the texture off): inert both times. A genuine, twice-measured negative under real render conditions, not merely \"never authored non-empty\" anymore - though not retested with `through_alpha` forced true, or on a shape where fill_style2 is confirmed to be the ACTIVE fill (this batch did not verify that; see this $def's own top-level note that no writer ever emits this object, so there is no reference behaviour to compare against)." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: path (Texture2 outer) real absolute image path, 113 sites (109 pre-stamped FileRef path sites, isolating this owner's own 4), .probe_texture2path_1mmsyqm_.animeproj frame 0, inert; retried with precondition fill_mode=1, same document/isolation, .probe_texture2path_fm1_9mphfjuz.animeproj frame 0, inert"},
        "SS_Texture2FileRef": { "$ref": "layer.schema.json#/$defs/FileRef", "description": "The same {relativeTo, path} shape used by every other file reference in the format. Observed {relativeTo: \"Absolute\", path: \"\"} on all 12 corpus sites/3 documents. M1.5 batch 7: unlike `path`/`relativeTo` (which collide with every other FileRef occurrence in the document, needing the pre-stamp isolation this $def's own `path` entry describes), the KEY NAME `SS_Texture2FileRef` itself is unique in the whole schema (confirmed by grep - exactly 1 declaration), so `tools/probe_field.py`'s own flat-key matching cannot cross-attribute a hit here - no isolation needed. Forced to a real, existing absolute image path on Others/IndependentAngle.animeproj's 4 real sites at frame 0: inert - consistent with the outer `path`'s own (isolated) inert finding one level up in the same object." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: SS_Texture2FileRef={relativeTo:Absolute,path:\"Images/Sky.png\"}, 4 sites, Others/IndependentAngle.animeproj frame 0, inert. Not read by moho2svg.py."},
        "fill_mode": { "type": "integer", "description": "Presumably tile/stretch/centre. Observed constant 0 across all 12 corpus sites/3 documents; other values not sampled, so the value set is unknown. Probed on Others/IndependentAngle.animeproj's own 4 real sites, forced to 1: inert." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: fill_mode=1, 4 sites, Others/IndependentAngle.animeproj frame 0, inert. Not read by moho2svg.py."},
        "through_alpha": { "type": "boolean", "description": "Same field name as Gradient.through_alpha, and a genuinely distinct owner in real files (see that entry's corpus scan: through_alpha spans both SS_Gradient2 and SS_Texture2 sites). Not registered here - unlike Gradient.through_alpha, neither svg2moho.py nor lottie2moho.py ever emits an SS_Texture2 object at all (M1.3 finding), so there is no writer-backed value to probe for this owner specifically; it needs M2's observe-then-probe recipe, the same as this $def's other undecoded fields. Observed constant false." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "no writer emits an SS_Texture2 object in either exporter (M1.3 finding, grep-confirmed) - the shared-name sibling Gradient.through_alpha IS writer-backed and registered EDITABLE, but that result cannot be copied onto this owner without a writer of its own; needs M2's observe-then-probe recipe instead of M1's."}
      },
      "additionalProperties": true
    },

    "ShadowStyle": {
      "type": "object",
      "description": "A per-shape drop-shadow effect on the stroke, used only as a line_style. Distinct from the LAYER-level shadow (LayerShadow in layer.schema.json), which has a different field set. Not decoded - not read by moho2svg.py.",
      "required": ["type"],
      "properties": {
        "type": { "const": "SS_Shadow" , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:5080 in Layer._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "angle": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Shadow direction in radians (observed constant 2.432966 across all 3 corpus sites - the rarest of this batch's fields, present on only 3 documents: Others/IndependentAngle.animeproj, Others/MaximumIKStrethching.animeproj, Others/TargetBone.animeproj). M1.5 batch 5: one of FOUR schema owners of this flat name - see Shaded.angle's own entry (this file) for the full owner list and the M1.4b precedent. Isolated on Others/TargetBone.animeproj by deleting the other three owners' `angle` sites first, forcing the remaining 1 ShadowStyle-only site from 2.432966 to a Val channel of 0.0 at frame 0: AFFECTS RENDER." , "x-moho-disposition": "EDITABLE", "x-moho-evidence": "docs/moho-field-probes.md: angle Val channel val 0.0, 1 site (isolated from LayerShading/LayerShadow/Shaded's own occurrences), TargetBone.animeproj frame 0, AFFECTS RENDER", "x-moho-render": "pending"},
        "offset": { "$ref": "channel.schema.json#/$defs/ValChannel", "description": "Shadow distance in Moho space units (observed 0.001786 - very small)." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:2981 in Bone._build (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "blur": { "$ref": "channel.schema.json#/$defs/ValChannel" , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "M1.4b: ShadowStyle is a shape-level style effect (SS_Shadow, a per-shape stroke drop-shadow) - a completely different subsystem from the layer-level LayerEffects/LayerShadow/LayerShading/PerspectiveShadow (all four writer-backed by lottie2moho.py and now EDITABLE, `.blur` in particular - see layer.schema.json's LayerEffects.blur). Neither svg2moho.py nor lottie2moho.py ever synthesizes an SS_Shadow object, so this owner has no writer-backed value to probe; needs M2's observe-then-probe recipe instead."},
        "color": { "$ref": "channel.schema.json#/$defs/ColorChannel", "description": "Observed as opaque black {0,0,0,1}." , "x-moho-disposition": "MODELLED", "x-moho-evidence": "moho2svg.py:7273 in Exporter._build_gradient (trace: out/traced_keys.json, see tools/trace_fields.py)"},
        "threshold": { "type": "boolean", "description": "Plain boolean, same as SS_Soft's own threshold field." , "x-moho-disposition": "UNKNOWN", "x-moho-unknown-reason": "M1.4b: ShadowStyle is a shape-level style effect (SS_Shadow) - a completely different subsystem from the layer-level LayerEffects/LayerShadow/LayerShading/PerspectiveShadow (all four writer-backed by lottie2moho.py and now EDITABLE - see layer.schema.json's LayerEffects.threshold). Neither svg2moho.py nor lottie2moho.py ever synthesizes an SS_Shadow object, so this owner has no writer-backed value to probe; needs M2's observe-then-probe recipe instead."}
      },
      "additionalProperties": true
    }
  }
}
